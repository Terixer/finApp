<div class="col-md-8 grid-margin stretch-card">
	<div class="card">
		<div class="card-body">
			{{ form_start(form) }}
			{{ form_widget(form) }}
			<button type="submit" class="btn btn-gradient-primary mr-2">Zapisz</button>
			<a href="{{path('period_index')}}" class="btn btn-light">Wróć do listy</a>
			{{ form_end(form) }}
		</div>
	</div>
</div>
